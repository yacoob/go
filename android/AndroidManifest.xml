<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="org.yacoob.trampoline"
          android:versionCode="1"
          android:versionName="1.0">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-sdk android:targetSdkVersion="10" android:minSdkVersion="10"></uses-sdk>
    <application android:icon="@drawable/icon" android:name="Hop" android:label="@string/app_name">
        <activity android:name=".HopListActivity" android:label="@string/activity_main">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".HopPreferences" android:label="@string/activity_preferences"/>
        <activity android:name=".HopPushActivity" android:theme="@android:style/Theme.NoDisplay">
            <intent-filter android:label="@string/push_popupmenu_entry">
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>
        <receiver android:exported="true" android:enabled="true" android:name=".HopWifiHandler">
            <intent-filter>
                <action android:name="android.net.wifi.STATE_CHANGE"/>
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE"/>
            </intent-filter>
        </receiver>
        <service android:name=".HopRefreshService"/>
    </application>
</manifest>
